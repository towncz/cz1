# 非程序开发要求总结

本文用于汇总课程作业中不直接属于程序编码的要求，包括报告写法、需要回答的问题、截图证据、提交细节和命名规范。

## 作业主题

作业主题为“我的家乡景点导览 App”。

报告和提交材料围绕家乡 6 个真实景点展开，说明如何通过 Android 四大组件完成一个完整的景点导览应用。

## 景点资料准备

需要自选家乡 6 个真实景点，覆盖自然、人文、休闲等类型：

- 公园
- 古迹
- 山水
- 街区
- 其他真实景点或地标

每个景点准备：

- 名称
- 50 字以内简介
- 类型：自然 / 人文 / 休闲
- 票价
- 是否开放

这些信息既用于程序，也用于报告说明和截图展示。

## 报告总体要求

报告应提交为 Word 文档。

每个部分都应包含：

1. 功能说明。
2. 运行截图。
3. Logcat 截图和运行证据。
4. 关键代码或关键实现思路说明。
5. 对应组件的原理解释。

报告必须结合图示，包括：

- 用例图
- 组件图
- 类图
- 序列图
- 状态图

所有图示均服务于功能说明，图中的类名、组件名、流程名必须与实际代码一致。

## Activity 部分报告需回答

需要说明：

1. 三个 Activity 的生命周期方法各在何时被调用。
2. 传递数据时使用了哪个 Intent 方法。
3. 为什么选择该 Intent 数据传递方法。

写作要求：

- 说明 `onCreate()`、`onStart()`、`onResume()`、`onPause()`、`onStop()`、`onDestroy()` 的调用场景。
- 结合从列表页跳转详情页、从详情页返回列表页的操作流程解释生命周期变化。
- 说明使用显式 Intent 跳转到详情页。
- 说明使用 `putExtra()` 传递景点名称和简介，因为数据简单、键值明确、适合 Activity 间轻量传参。

## Service 部分报告需回答

需要说明：

1. Started Service 和 Bound Service 的生命周期区别。
2. 为什么票价查询选用绑定式而不是启动式。

写作要求：

- 用表格和流程图比较 Started Service 与 Bound Service。
- Started Service 更适合“开始后独立运行”的任务，例如后台讲解。
- Bound Service 更适合“组件调用服务方法并立即获得结果”的场景，例如票价查询。
- 票价查询需要 Activity 与 Service 直接交互，调用 `getTicketPrice()` 后立刻返回结果，因此使用绑定式更合理。

## BroadcastReceiver 部分报告需回答

需要说明：

1. 静态注册和动态注册的使用场景区别。
2. 为什么 `NetworkReceiver` 要在 `onPause()` 注销而不是 `onDestroy()`。

写作要求：

- 静态注册适合即使 App 未打开也希望接收的系统广播，例如开机广播。
- 动态注册适合只在页面可见或活跃时监听的事件，例如 MainActivity 中监听网络变化。
- `onPause()` 注销可以避免页面不可见时继续接收广播，减少资源消耗，并避免 Activity 泄漏。
- `onDestroy()` 不一定会及时调用，因此不适合作为网络监听注销的唯一时机。

## ContentProvider 部分报告需回答

需要说明：

1. ContentProvider 相比直接使用 SQLiteDatabase 的优势。
2. URI 中 authority 的作用，以及为什么要全局唯一。

写作要求：

- ContentProvider 为数据访问提供统一接口，便于不同组件或其他应用通过 ContentResolver 访问数据。
- 它封装了数据库细节，使外部调用方不需要直接操作 SQLiteDatabase。
- 它更适合跨组件、跨进程或跨应用共享数据。
- authority 用于唯一标识某个 ContentProvider。
- authority 必须全局唯一，否则系统无法准确判断 URI 应交给哪个 Provider 处理。

## 截图要求

每个功能点都需要运行截图和对应的 Logcat 证据。

必须准备以下截图：

1. App 主页面和主菜单。
2. 景点列表页。
3. 点击景点后的详情页。
4. Intent 传参显示结果。
5. 返回列表演示。
6. 开始讲解后的 Logcat 输出。
7. 停止讲解后的操作结果。
8. 票价查询 Toast。
9. 网络断开和网络恢复 Toast。
10. 自定义广播接收后的 Logcat。
11. BootReceiver 通知和相关配置说明截图。
12. ContentResolver 查询全部景点 Log。
13. ContentResolver 插入、删除、更新的 Log 和结果截图。
14. AboutActivity 个人主页。

## 提交要求

提交内容包括：

1. Android Studio 工程压缩包。
2. Word 报告文档。
3. 完整截图材料，截图需整理进报告。

代码工程要求：

- 打包为 zip。
- 能正常编译。
- 能正常运行。
- 已执行 `Clean Project` 并通过。
- 已执行 `Rebuild Project` 并通过。

## 文件命名规范

提交文件名格式：

```text
班级_学号_姓名_大作业
```

zip 和 Word 报告保持同一命名主体，例如：

```text
软件工程XXX班_2023123456_张三_大作业.zip
软件工程XXX班_2023123456_张三_大作业.docx
```

## 报告结构

Word 报告按以下结构组织：

1. 封面：课程名、作业名、班级、学号、姓名。
2. 作业主题与需求概述。
3. 景点数据说明。
4. 系统总体设计。
5. Activity 模块实现与说明。
6. Service 模块实现与说明。
7. BroadcastReceiver 模块实现与说明。
8. ContentProvider 模块实现与说明。
9. 运行截图与 Logcat 证据。
10. 遇到的问题与解决方法。
11. 总结。

## 最终检查清单

提交前检查：

1. 报告中四个组件部分都已覆盖。
2. 每个功能点都有截图或 Logcat 证据。
3. 需要回答的理论问题都已回答。
4. 工程能 Clean 和 Rebuild。
5. 工程压缩包不是只包含 APK，而是完整 Android Studio 工程。
6. 文件命名符合 `班级_学号_姓名_大作业`。
7. 报告中的截图、类名、功能名与实际代码一致。
